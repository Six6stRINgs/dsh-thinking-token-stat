# 更新记录

每个发布版本一行。1.0.2 之前的版本仅用于开发，从未发布到 npm。

## 1.1.0 —— 2026-09-13

改用本插件自己注册的会话投影重构，并把每个数字的措辞与覆盖范围都写明白。浏览器半边
不再读取对话本身。

- 改为会话级统计：比例只覆盖**真正思考过的轮次**，因此切换到不思考的模型后，已有数字
  不会再往下滑。
- 新增覆盖度（`4 / 6 轮`，仅在会话中有一部分轮次没思考时显示），以及一行说明总量里有多少
  是由思考文本推算、而非由 provider 上报。
- 移除单条回复旁的读数：DSH 官方回复面板已经给出该轮的推理 token 数，属于重复。
  插件现在只保留一个读数。
- 明细里新增逐轮视图：每轮一行，包含该轮思考 token、本轮总输出 token、这个数字的来源、
  该轮跑的模型，以及它占本轮输出的比例；上方三个来源筛选开关，每行末尾一个跳转按钮。
- 修复模型列全为空的问题：官方的逐轮用量数据在真实日志里只有少数几轮存在，现在模型名来自插件
  自己的折叠——每条结算事件都自带 provider/model。
- **改用会话投影重构。** 宿主半边把整条会话事件日志折叠一次（只认 `assistant/message` 结算，
  每轮六个数字，模型名走字典），注册成 `thinkingStats` 投影；浏览器半边只读这一个值。因此这些
  数字天然是整场的——分页和压缩都改不动它们——刷新后也完全一致。
- **加载整套机制都删掉了**：逐轮缓存、"读取更早的一页 / 连读 5 页"、以及浏览器 local storage
  全部移除。已经没有什么要加载的：宿主早就折完了。客户端不再需要会话服务、会话绑定、对话窗口、
  节点存储，也不再需要轨迹折叠。
- 实测（一场真实的 88 轮会话）：8 988 条事件总共约 5 毫秒折完（每事件 0.6 微秒），落盘状态
  3.0 KB（每轮 36 字节），发布给客户端的视图 2.4 KB——比 DSH 自己为同一场会话保存的
  `turnOutline` 还小。
- 总览里不做"按模型拆分"：表格的模型列已经逐轮回答了同一个问题，再做一份聚合就要为重复的
  信息多维护一份状态。
- 逐轮表格其余部分不变（列、来源筛选、每页 100 轮、跳转按钮），同时估算行的思考与占比现在都带 `~`
  并用蓝色标出。
- 总览的统计范围那一行改为按折叠结果**算出来**，而不是写死：整场会话加总轮数；若有不产出消息的
  轮次、或是 fork 出来的会话、或宿主根本没给逐轮投影，就分别写明。只要不是完整统计，就带一个
  `?`，用与估算同款的消息说明原因；逐轮明细自身的上限也有一个 `?`。
- 折叠改为在 `turn/start` 边界就开出一轮，而不是等到第一次结算：报错或被中断、因而从未产出消息
  的轮次现在也会列出（各列都是 `—`），不会看起来像对话少了一轮。折叠同时记录实际结算过的轮数，
  统计范围那一行的缺口就是用它算的。
- 推算值改为**按语种取密度**，不再一刀切按 4 字符/token：中日韩文字、假名、谚文与全角形式按
  1 字符/token，西里尔、希腊、阿拉伯、希伯来、亚美尼亚、印度诸文字、泰文与格鲁吉亚文按 2.5，
  拉丁字母/数字/标点按 4。每个思考块两次 `replace`，实测在一场会话 230 万字符的思考文本上多花
  1.7 毫秒（折叠总共 5 毫秒）。折叠的 `stateVersion` 升到 2，旧检查点会被重折而不是混用。
- 估算那一行改为 `估算约 5.1K token`，不再只给一个光秃秃的 `~5.1K token`，让这个数字读起来
  就是一句"这是估算"。
- 宿主不提供该投影时，读数退回官方的整场 token 投影并写明原因，而不是凭空编造逐轮数字。
- 新增折叠的单元测试 `test/projection.mjs`：给 `DSH_TEST_LOG` 时，它会重折一场真实录制的会话，
  并把每项合计与对同一批事件的独立统计逐项核对。
- 表格改为**每次只渲染一页 100 轮**，用「更新的 / 更早的」和页码翻页，只有当前页参与排版。
- 修复逐轮表格在英文下的表头：列宽是按更短的中文标签定的，导致 `Thinking source` 被截成
  `Thinking sou…`、`Of output` 折成两行。现在这两列按中英文里更长的那个标签取宽，且任何
  表头单元格都不再换行。
- 修复总览脚注里的 `?`：跟在句子后面时它需要自己的间距，而不是贴在句号上。
- 修复估算说明卡触底：它是一段散文，高度随语言和字数变化，现在改为**量出实际高度**再把卡片
  夹进视口内，不再按一个比真实卡片矮的固定高度去摆放；窗口真的放不下时卡片内部滚动，而不是
  被裁掉。摆放也不再每次渲染都重算一个新的位置。
- 跟随界面语言，且只写 DSH 自带的两种：只有 `zh` 开头的 `<html lang>` 显示中文，其它任何值
  ——第三方语种，或语言服务尚未发布时的空属性——一律显示英文，这也正是 DSH 自己的回退。切换
  语言会立即更新读数，无需刷新。
- 修复关闭逻辑：点界面以外或按 Esc 之前完全关不掉，因为关闭时把 view 设成了渲染判断不认为是
  "已关闭"的值，于是只有点 logo 才能关。现在一级、二级都能从任何位置关闭，并且监听放在捕获
  阶段，别的组件 stopPropagation 也吞不掉它。
- 所有由文本推算得出的数字都加 `~`：输入框那行的读数、两个比例、表格上方的合计，以及表格
  里估算行的思考与占比（后两格还会用蓝色标出）。
- 估算旁边的 `?` 改成在面板旁边**单独弹出一条说明**，而不是触摸屏上根本出不来的悬停提示。
- 统计范围说明放在明细面板最后一行，位于"进入逐轮视图"那一行之后，并写明数字覆盖什么。
- README 改为平实说法，并更换截图。

## 1.0.8

- README：补充 npm 安装方式。

## 1.0.7

- 明细面板跟随 DSH 界面语言（中文 / 英文）；每行标注该比例对应的总量。
- 更换截图。

## 1.0.6

- 用与 DSH 风格一致的明细面板取代原来的悬停提示。

## 1.0.5

- 单条回复旁的读数改为与内置一致的图标加文字形式。

## 1.0.4

- 跟随新版 DSH 的回复尾部样式，使读数与内置读数对齐。

## 1.0.3

- 项目与包名改为 `dsh-thinking-token-stat`，并更新所有仓库链接。
- 说明不暴露思考的模型不会显示读数。

## 1.0.2

- 首个 npm 发布版本，使用当前包名。
