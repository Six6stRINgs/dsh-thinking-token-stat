# This is the ONE file you add to your pull request on
# awesome-dsh-plugin/awesome-dsh-plugin.
#
#   target path in that repo:  data/plugins/Six6stRINgs__dsh-client-ui-thinking-stats.yml
#
# Do NOT hand-edit the list's README files — they are generated from these
# entry files.
url: https://github.com/Six6stRINgs/dsh-client-ui-thinking-stats
name: Six6stRINgs/dsh-client-ui-thinking-stats
category: usage
description:
  en: Lightweight thinking (reasoning) token stats for the DSH web GUI — a client-only session-total dock readout and a per-turn readout on each reply's timing row, showing the count and its share of all and output tokens; hidden when there is no thinking.
  zh: 轻量的 thinking（推理）token 统计插件（纯前端）— 在底部 dock 显示会话累计值、在每条回复的计时行显示本轮值，展示数量及其占全部 token 与输出 token 的比例；无思考时不显示。
